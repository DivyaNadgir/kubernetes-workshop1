apiVersion: v1
kind: Pod
metadata:
  name: nginx-demo.example.com
  labels:
    app: nginx_demo
spec:
  containers:
  - name: k8s-nginx-demo
    image: nginx
    ports:
    - name: nginx-port
      containerPort: 80
